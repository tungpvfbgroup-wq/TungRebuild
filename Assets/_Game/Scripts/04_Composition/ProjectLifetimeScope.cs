using VContainer;
using VContainer.Unity;

namespace Tung.Composition
{
    public class ProjectLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
