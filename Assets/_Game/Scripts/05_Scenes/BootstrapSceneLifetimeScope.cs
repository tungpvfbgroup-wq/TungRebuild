using VContainer;
using VContainer.Unity;

namespace Tung.Scenes
{
    public class BootstrapSceneLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
